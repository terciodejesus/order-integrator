export type Address = {
  name: string;
  street: string;
  streetNumber: string;
  complement?: string;
  district: string;
  city: string;
  uf: string;
  zipCode: string;
  phone: string;
  taxIdentification?: string;
};
